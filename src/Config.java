//The config file should include the IP address, port number, and ID (e.g., S1, A) of each device, and how they are connected.

//The config file should also specify how the devices are connected, i.e., it should include all the links.

public class Config {
}
